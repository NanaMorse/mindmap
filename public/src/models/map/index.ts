const mapModel: mapModelType = {
  namespace: 'map',

  state: {

  },

  reducers: {
    updateTopicTitle(state) {
      return state
    },

    updateTopicFontSize(state) {
      return state
    },

    updateTopicTextColor(state) {
      return state
    },

    updateTopicIsTextBold(state) {
      return state
    },

    updateTopicIsTextLineThrough(state) {
      return state
    },

    updateTopicShapeClass(state) {
      return state
    },

    updateTopicBorderWidth(state) {
      return state
    },

    updateTopicBorderColor(state) {
      return state
    },

    updateTopicLineClass(state) {
      return state
    },

    updateTopicLineWidth(state) {
      return state
    },

    updateTopicLineColor(state) {
      return state
    },

    updateTopicLabelText(state) {
      return state
    },

    addChildTopic(state) {
      return state
    },

    addParentTopic(state) {
      return state
    },

    removeSelf(state) {
      return state
    }
  }
};

export default mapModel