import { appState } from 'src/interface'

const appModel = {
  namespace: 'app',

  state: <appState> {
    // info item的显示方式
    infoItemDisplay: {}
  },

  reducers: {

  }
};

export default appModel;